{
  "name": "good",
  "version": "1.0.0",
  "description": "Design system tooling: DTCG tokens → CSS, WCAG contrast checks, visual regression",
  "type": "module",
  "private": true,
  "scripts": {
    "build:tokens": "node scripts/build-tokens.mjs",
    "check:contrast": "node scripts/check-contrast.mjs",
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots",
    "verify": "npm run build:tokens && npm run check:contrast && npm run test:visual"
  },
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "style-dictionary": "^5.5.0"
  }
}
